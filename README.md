# Python-data-structure-and-algorithms

## 常见的大O数量级函数

| 函数 | O(1) | O(log(N)) | O(N) | O(N*log(N)) | O(N^2) | O(N^3) | O(2^N) |
| ---- | ---- | --------- | ---- | ----------- | ------ | ------ | ------ |
| 名称 | 常数 | 对数      | 线性 | 对数线性    | 平方   | 立方   | 指数   |

1.一般将赋值语句的操作作为一个单位时间

2.发现问题规模

3.计算赋值语句的执行次数，然后取主导项衡量时间复杂度

## 栈(Stack)

一种有次序的数据项集合。在栈中，数据项的加入和移除都仅发生在同一端。

* 特点为

1. **后进先出(Last in first out, LIFO)**：最新加入栈的数据项会被最先移除
2. **反转次序**：进栈和出栈的次序相反

* 相关操作

1. stack(): 创建一个空栈，不包含任何数据项
2. push(item): 将item加入栈顶，无返回值
3. pop(): 将栈顶数据项移除，并返回，栈被修改
4. peek(): 获取栈顶数据项，返回栈顶数据项但不移除，栈不被修改
5. isEmpty(): 返回栈是否为空栈
6. size(): 返回栈中有多少个数据项

## 队列(Queue)

一种有次序的数据集合，其特征是新数据项的添加总是发生在一端（通常称为尾端，rear），而现存数据项的移除总发生在另一端（通常称为首端，front）

* 特点为

1. 先进先出（FIFO：first-in first-out）
2. 只有一个入口和出口

* 相关操作

1. Queue(): 创建一个空队列对象，返回值为Queue对象
2. enqueue(item): 将数据项item添加到队尾，无返回值
3. dequeue(): 从队首移除数据项，返回值为队首数据项，队列被修改
4. isEmpty(): 测试是否为空队列，返回值为bool型
5. size(): 返回队列中数据项的个数

## 双端队列(Deque)

* 相关操作

1. Deque(): 创建一个空双端队列对象，返回值为Queue对象
2. addFront(item): 将item加入队首
3. addRear(item): 将item加入队尾
4. removeFront(): 从队首移除数据项，返回被移除的数据项，双端队列被改变
5. removeRear(): 从队尾移除数据项，返回被移除的数据项，双端队列被改变
6. isEmpty(): 返回deque是否为空，bool型
7. size(): 返回deque中包含数据项的个数

## 无序表(UnorderedList)

用链表实现，一般不占用连续内存。

* 相关操作

1. add
2. remove
3. search
4. 等等

## 有序表(OrderedList)

用链表实现，一种数据项依照某个可比性质（如整数大小、字母表先后）来决定在列表中的位置

* 相关操作

1. OrderedList()：创建一个空的有序表
2. add(item)：在表中添加一个数据项，并且保持整体顺序
3. remove(item)：从有序表中移除一个数据项，并且此项应该存在，同时有序表被修改
4. search(item)：在有序表中查找数据项，并且返回是否存在，bool
5. isEmpty()：是否为空表，bool
6. size()：返回表中数据项的个数
7. index(item)：返回数据项在表中的位置，此项应存在
8. pop()：移除并返回有序表中最后一项，表中应至少存在一项
9. pop(pos)：移除并返回有序表中指定位置的数据项，此位置应存在

## 散列(hashing)


## 树
